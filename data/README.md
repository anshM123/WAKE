# WAKE data

Raw sessions live under `data/sessions/YYYYMMDD_HHMMSS/` and are intentionally
ignored by Git. Never overwrite raw streams with inferred values. Archive each
session with its metadata, configuration snapshot, and model artifacts.
